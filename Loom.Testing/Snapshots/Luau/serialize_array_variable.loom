[serializable]
interface Roster {
    names: string[];
    positions: Vector3<i16>[];
}

fn _trigger_usage(): void {
    let payload = serialize_binary(none as never as Roster);
    let restored = deserialize_binary::<Roster>(payload);
}
