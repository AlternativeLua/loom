[serializable]
interface Flags {
    kind: "spawn";
    alive: bool;
    ready: bool;
    [number_range(0, 100)]
    health: number;
}

fn _trigger_usage(): void {
    let payload = serialize_binary(none as never as Flags);
    let restored = deserialize_binary::<Flags>(payload);
}
