[serializable]
interface Payload {
    origin: Part;
    handler: fn(n: number): void;
    extra: unknown;
    target: Instance;
}

fn _trigger_usage(): void {
    let payload = serialize_binary(none as never as Payload);
    let restored = deserialize_binary::<Payload>(payload);
}
