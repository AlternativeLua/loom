[serializable]
interface Marker {
    kind: "spawn";
    target: Part;
}

fn _trigger_usage(): void {
    let payload = serialize_binary(none as never as Marker);
    let restored = deserialize_binary::<Marker>(payload);
}
